export = getParameterValuesFromUIControls;
/**
 * extracts the parameter
 * @param {Array} paramControls
 * @param {Boolean} onlyChanged
 * @returns {Object} the parameter values, as an object
 */
declare function getParameterValuesFromUIControls(paramControls: any[], parameterDefinitions: any, onlyChanged: boolean): any;
