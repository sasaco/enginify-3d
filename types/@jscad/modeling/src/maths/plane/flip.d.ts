export = flip;
/**
 * Flip the given plane.
 *
 * @param {plane} out - receiving plane
 * @param {plane} plane - plane to flip
 * @return {plane} out
 * @alias module:modeling/maths/plane.flip
 */
declare function flip(out: typeof plane, plane: typeof plane): typeof plane;
